interface:
  display_name: "Polylane"
  short_description: "Build an app 0→100 from one prompt via parallel Codex lanes"
  default_prompt: "Use $polylane to build and continuously improve this product with parallel Codex CLI lanes."
policy:
  allow_implicit_invocation: true
