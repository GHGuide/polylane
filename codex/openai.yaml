interface:
  display_name: "Polylane"
  short_description: "Turn a repository-backed project goal into verified outcomes"
  default_prompt: "Use $polylane to plan and execute this repository-backed project with parallel Codex CLI lanes and truthful evidence."
policy:
  allow_implicit_invocation: true
