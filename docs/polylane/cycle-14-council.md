# Cycle 14 council — external evidence only

The product, reliability, security, simplicity, and adversarial seats unanimously
accept the cycle-14 engineering result. Promotion and reporting are transactional,
liveness no longer kills legitimate quiet work, worker history is canonical, selected
skills reach builders with auditable receipts, and the physical GO/NO-GO lifecycle is
green. The goal tree has no open autonomous subgoal and all 51 frozen acceptance checks
pass. The council therefore rejects a speculative cycle 15: the mechanically correct
route is `EXTERNAL-EVIDENCE-OPEN` for `m12.4` / `c28`, whose ten-product rendered blind
comparison requires evidence the engineering loop cannot honestly self-award.

Drift audit: the result remains aligned with the north star—one unattended first run,
truthful state, bounded recovery, and parity across Claude and Codex. Skill audit: the
cycle's selected skill-delivery mechanisms now require readable trusted paths and actual
use receipts; no skill was credited merely for being named in a prompt.
