# Cycle 40 council — elected continuation

The council elected source acquisition and held-out calibration as the sole highest-value
continuation. More validator scaffolding would not improve the claim while the primary
human-labelled bytes are absent. The next cycle must fix the observed Chrome readiness
and cross-origin download seams, use immutable DataONE metadata only as an independent
provenance cross-check, freeze the source split before model output, then run and audit at
least five declared judge configurations. The generation wave starts only after one
deterministic preflight closes that evidence.

The council rejected fixture substitution, TASTE-as-primary, post-result item replacement,
holdout tuning, and any `human_certified:true` claim. Verified engineering may continue
under `EXTERNAL-EVIDENCE-OPEN`; the machine-panel ceiling remains
`HUMAN_CALIBRATED_MACHINE`.

