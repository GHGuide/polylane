# Cycle 30 per-lane skill suggestions

No new skill install is recommended. The failure is fully reproduced and the
relevant installed skills already cover diagnosis, tests, and independent review.

## Builder: `gate-truth`

1. `superpowers:systematic-debugging` — trace the witnessed READY, acceptance,
   evidence, promotion, and report state transitions before changing policy.
2. `superpowers:test-driven-development` — freeze phantom counting, inherited
   failure variables, stale evidence cleanup, duplicate execution, and exact
   report attribution before production edits.

## Integrator

3. `superpowers:verification-before-completion` — require current exact-tip,
   focused, ShellCheck, and zero-terminal-gate evidence before GO.
4. `engineering:code-review` — independently audit environment isolation,
   race resistance, Bash 3.2 compatibility, and fail-closed promotion behavior.

Considered but not selected: `engineering:debug` overlaps systematic debugging;
`engineering:testing-strategy` adds breadth without a missing test category;
`ponytail:ponytail-review` is optional if the repair introduces a new abstraction.
GitHub acquisition is declined because it cannot improve this known local shell
contract within the cycle and would add unrelated supply-chain work.
