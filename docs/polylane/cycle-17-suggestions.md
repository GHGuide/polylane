# Cycle 17 skill suggestions

All selected skills are already installed; no network install is needed. `None`
remains valid, but the following small kits directly match the observed failures.

## `gate-contracts`

- `superpowers:systematic-debugging` — reconstruct the exact control-flow leak before changing mocks.
- `engineering:testing-strategy` — keep gate tests hermetic while preserving integration coverage.
- `engineering:incident-response` — treat false worker restarts and a hung host gate as recovery incidents with durable evidence.

## `skill-contracts`

- `superpowers:test-driven-development` — reproduce benchmark-free arming before changing the fixture or implementation.
- `engineering:code-review` — verify the repair cannot weaken admission or silently bless unmeasured skills.
- `operations:risk-assessment` — examine stale fingerprints, forged receipts, and unsafe recommendation defaults.

## Integrator

- `superpowers:verification-before-completion` — reserve the full terminal matrix for the coordinator boundary.
- `engineering:code-review` and `ponytail:ponytail-review` — review both repairs for security regression and needless abstraction.
