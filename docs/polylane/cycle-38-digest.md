# Cycle 38 digest — evidence primitives GO

Cycle 38 promoted the executable primitives for browser-capture manifests, decoded
pixel checks, held-out calibration, pointwise-before-pairwise ballots, statistical
aggregation, threat review, and fail-closed certificate compilation. Its focused
tests and ShellCheck passed, so `m32.2` is complete.

That result is deliberately narrower than production taste certification. The
post-cycle adversarial swarm found that the live runner still used the legacy
visual-quality boundary and that several v1 receipts could be hand-authored with
the right shape. Cycle 38 therefore proves useful primitives, not a real benchmark,
human panel, production-authoritative receipt chain, or certified UI. Cycle 39 must
close those seams before any old-versus-new claim is allowed.
