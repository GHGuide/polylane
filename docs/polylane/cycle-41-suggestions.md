# Cycle 41 skill suggestions

The lane scout ran before prompt generation. Its live GitHub search surface was
unavailable, so no unreviewed repository was installed or armed. The cycle uses only
installed, fingerprinted skills that map directly to its work:

- `superpowers:test-driven-development` — write transport, parser, resume, and
  fail-closed regressions before implementation.
- `engineering:testing-strategy` — separate hermetic unit tests, local campaign
  rehearsals, and the one real source/calibration run.
- `engineering:debug` — diagnose WAF readiness, CDP downloads, redirects, corrupted
  objects, and provider failures from evidence rather than retries by guesswork.
- `operations:risk-assessment` — protect the provenance, credential, mirror,
  substitution, and claim-label trust boundaries.
- `deep-research` — used only by the source/protocol research lane to reconcile primary
  documentation and immutable source identities.

Decision: arm these installed skills per lane. Install none. Re-run GitHub discovery in
a later cycle only if a concrete unresolved activity remains and benchmark any candidate
before admission.

