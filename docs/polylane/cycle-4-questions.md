# Cycle 4 questions

No product-direction answer is required. Autonomous defaults: coordinator owns host-only checks;
unknown usage is reported as unknown; interrupted work is resumed from committed evidence;
missing panes are recreated rather than retried by index; workers receive only selected installed
skills; terminal suites run once at the outer gate; and the efficiency canary is not allowed any
manual repair.
