# Cycle 39 council — elected continuation

The evidence tree routes to `m32.4`; the council cannot stop or certify the project.
The elected next focus is a production benchmark harness before any large generation
wave. It must acquire the named human-labelled corpus with pinned provenance, calibrate
provider configurations on a frozen holdout, render and hard-gate real candidates, mint
non-fixture ballot-v2 receipts from raw isolated responses, and compile a study manifest
without weakening the preregistered thresholds.

The council rejected three shortcuts: treating Cycle 39 fixtures as live evidence,
calling TASTE a silent substitute for the protocol's primary Miniukovich–Figl corpus,
and calling machine votes human certification. TASTE may be a separately pinned
secondary audit. The attainable claim remains `HUMAN_CALIBRATED_MACHINE` with
`human_certified:false`; only real deciding human ballots can raise it.
