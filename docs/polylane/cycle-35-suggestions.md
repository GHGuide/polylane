# Cycle 35 skill suggestions

- `superpowers:verification-before-completion` (installed, selected): require exact
  package-tree evidence rather than checking only that entrypoints exist.
- `engineering:testing-strategy` (installed, selected): design a legacy-destination
  regression fixture for both Codex roots and Claude Code.
- GitHub/external: none. The defect and acceptance surface are fully local and
  deterministic.
