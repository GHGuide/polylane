# Cycle 2 council

Promote the graph and event substrate: its contract, replay, and runner parity are proven.
Do not yet claim that Polylane is graph-executed, because the runner still makes launch
decisions first and records them afterward. The next cycle must make graph readiness a
precondition for every launch and route, optimize the measured ledger hot path without
weakening replay correctness, and harden the runtime failures observed during this real
cycle. The authority change is accepted only if negative tests prove a blocked dependency
prevents tmux side effects and the frozen benchmark passes on the same production APIs.

