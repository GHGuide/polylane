# Cycle 34 per-lane skill suggestions

All selected skills are installed and fingerprinted. A fresh certificate must not
introduce an unmeasured GitHub dependency.

## Builder: `terminal-certification-audit`

1. `superpowers:verification-before-completion` — require exact promoted-tip and
   frozen-inventory evidence before READY eligibility is claimed.
2. `engineering:testing-strategy` — distinguish focused audit checks from the one
   runner-owned terminal matrix and avoid consuming the latter early.

## Integrator

3. `engineering:code-review` — independently audit the retained repair ancestry,
   provider/install surfaces, and exact builder handoff.
4. `superpowers:verification-before-completion` — require two launches, zero
   restarts, zero pre-handoff terminal gates, and a clean exact tip before READY.

Selected: all four installed skills. GitHub suggestions/new installs: none.
