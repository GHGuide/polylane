# Cycle 27 emergent questions

No user decision blocks autonomous continuation.

1. Does a fresh Cycle 28 process pass the one terminal boundary after loading all
   Cycle 27 functions? Default: run the frozen full suite, ShellCheck, provider
   parity, both installers, and live GO/NO-GO rehearsal exactly once. Deeper next
   round: if it fails, retain the bounded current-run command tail and open a new
   nonce focused only on the first failed contract.
2. Should acceptance evidence keep the historical `.jsonl` suffix even though the
   atomic file is a JSON array? Default: preserve the compatible path because its
   schema is validated and reports link it correctly. Deeper next round: version a
   `.json` rename only with migration and stale-link regression coverage.
3. Should the staged Graphify artifact be refreshed after Cycle 28 certification so
   new `kit_active` and normalization-scope nodes become navigable? Default: do not
   rebuild during certification. Deeper next round: refresh only after GO, then
   compare query coverage and graph budget against the frozen baseline.
