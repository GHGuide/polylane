# Cycle 3 council

Accept the authoritative graph runtime and its measured performance. The scheduler, replay,
route gating, corruption fallback, workspace-write boundary, and post-promotion recovery are
strong enough to promote. Continue because the final trust claim is broader than scheduler
correctness: the live rehearsal still bypasses contract v2, status cleanup dirties the promoted
tree, paywall observation has a demonstrated false positive, and report suggestions are not
run-scoped. The next cycle should close those exact seams with a real supervised two-lane Codex
run, then execute every terminal acceptance check without weakening or relocating it.
