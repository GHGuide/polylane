# Cycle 4 council

Accept the walk-away correctness proof and dual-jq graph budget, but do not call the pipeline
efficient yet. Continue on the measured critical path: native pane recreation, inactive-agent
detection, explicit coordinator-owned host gates, writable per-lane check caches, selected-skill
prompt kits, cumulative resume-safe telemetry, and stale-worktree cleanup. Freeze a final canary
that permits no manual tmux commands, one terminal gate, truthful nonzero-or-unknown metrics, and
a clean teardown. Stop only when that canary passes; do not spend another cycle optimizing the
already-fast graph packet.
