# Cycle 24 per-lane skill suggestions

All useful skills are already installed; this cycle installs nothing and adds no
GitHub supply-chain surface.

## `pane-identity`

- **Selected — `superpowers:test-driven-development`:** pin cwd drift and wrong-run
  tag behavior red before changing the shared tmux identity API.
- **Selected — `engineering:debug`:** separate socket, session, cwd, and pane-tag
  hypotheses with a small real-tmux reproduction.

## `context-hygiene`

- **Selected — `superpowers:test-driven-development`:** freeze stale-run filtering,
  exact inbox syntax, and navigation-only skill rejection.
- **Selected — `engineering:testing-strategy`:** cover scoped and legacy worker
  semantics without weakening cross-worktree authority.

## `runner-wire`

- **Selected — `superpowers:verification-before-completion`:** wire both APIs only
  after focused evidence and prove every launch/adopt/recreate boundary.
- **Selected — `engineering:architecture`:** preserve one authority for pane identity
  and one explicit nonce for worker continuity while separating advisory manifest
  metadata from CLI remap operations.

## `integrator`

- **Selected — `superpowers:verification-before-completion`:** issue READY only from
  current exact-tip and focused evidence.
- **Selected — `engineering:code-review`:** independently review fail-closed identity,
  stale-context isolation, shell portability, and prompt-size effects.

Graphify is deliberately not selected. The already-refreshed read-only graph is
queried through `graphify-out/q.py`; loading its 37,063-byte build/install manual in
every lane cannot improve those queries.
