# Cycle 42A skill suggestions

The per-lane scout ran before prompt generation. Under the strict automatic
recommendation policy, no skill has the required same-fingerprint three-run hard-pass
ledger, so the automatic recommendation for every lane is `None`. No GitHub repository
is installed or executed in this cycle.

Autonomous mode may arm already-installed, trusted skills after a manual evidence review.
The smallest useful kits are:

- `execution-contract-freeze`: `superpowers:test-driven-development`,
  `engineering:system-design`, `engineering:testing-strategy` — executable schemas,
  boundary design, and fail-first conformance tests.
- `evidence-policy-freeze`: `superpowers:test-driven-development`,
  `operations:risk-assessment`, `engineering:testing-strategy` — adversarial trust
  propagation, claim ceilings, and reproducible statistics.
- `source-contract-freeze`: `superpowers:test-driven-development`,
  `data:validate-data`, `data:statistical-analysis`, `operations:risk-assessment` —
  source invariants, session/duplicate handling, and calibration thresholds.
- `lifecycle-external-routing`: `superpowers:systematic-debugging`,
  `superpowers:test-driven-development`, `engineering:testing-strategy`,
  `operations:risk-assessment` — reproduce Cycle 41 first, then repair transaction and
  watchdog behavior without weakening safety.
- `taste-contract-integrator`: `engineering:architecture`, `engineering:code-review`,
  `engineering:testing-strategy`, `operations:risk-assessment` — cross-contract review
  and independent gate execution.

Decision: arm these installed local kits. Install none. `graphify` remains read-only
navigation infrastructure supplied once by the orchestrator; it is not a builder skill.

