# Cycle 40 emergent questions

No user decision is required before the next autonomous cycle. The following questions
are answerable by implementation and evidence:

1. Can the WAF be handled by observed readiness in a fresh ephemeral Chrome context,
   without personal cookies, fingerprint spoofing, credential copying, or CAPTCHA work?
2. Can redirected data files be downloaded through supported CDP/session mechanics and
   atomically resumed without challenge HTML or partial bytes entering the cache?
3. Do Harvard metadata and DataONE's immutable metadata objects agree exactly on all
   three DOIs, versions, licences, file identities, sizes, and distributions?
4. Can the real rating schemas be normalized without guessing, then deterministically
   produce the frozen 60/24-per-domain split and unambiguous mirrored pairs?
5. Which five or more declared Claude/Codex configurations pass the frozen 24-pair,
   Wilson, side-bias, mirror, provenance, and session-isolation gates?
6. Does one fail-closed preflight prove that the expensive twenty-brief generation wave
   can begin without changing any preregistered input or threshold?

