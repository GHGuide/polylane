# Cycle 33 per-lane skill suggestions

All selected skills are already installed and fingerprinted. No GitHub search or
installation is useful for this narrow local Bash contract.

## Builder: `efficiency-contract-repair`

1. `superpowers:systematic-debugging` — trace the contradictory report to the
   exact final-certificate branch instead of changing cleanup behavior.
2. `superpowers:test-driven-development` — freeze focused `0/0`, terminal `1/1`,
   mismatch rejection, and legacy proof compatibility before implementation.

## Integrator

3. `engineering:code-review` — audit numeric parsing, fail-closed verification,
   Bash 3.2 compatibility, and the absence of terminal-boundary weakening.
4. `superpowers:verification-before-completion` — require exact-tip focused proof,
   zero restarts, zero terminal gates, truthful final cleanup, and criterion closure.

Selected: all four installed skills. New installs: none.
