# Cycle 18 council — truthful walk-away recovery

## Decision

Accept the merged runtime-resilience and skill-context repairs as locally verified for
`c53`, `c54`, `m18.1`, and `m18.2`. The integrator independently found, red-tested, and
closed the only cross-lane seam: the runner now calls the selected-skill compiler with
the typed lane JSON kit, so builders receive exact trusted records rather than a
best-effort skill path. Focused integration evidence for `m18.3` is also current.

## Safety rulings

- Never send tmux input to prose that merely mentions a prompt; a live numbered option
  is required.
- Keep terminal host-failure evidence in the canonical host root, not a completed lane
  worktree; preserve atomic report/event behavior and bounded disk backoff.
- Preserve benchmark admission, immutable skill fingerprints, observable `SKILL-READ`
  receipts, same-repository-only graph sharing, approval hashes, and paper-only trading.
- Treat the graph query as an aid, not a substitute for current fixture evidence when
  the existing graph predates changed helpers.

## Verdict posture

`READY-FOR-HOST-GATE` is warranted for `c18-walkaway-truth-20260809-a1` only because the
local and cross-contract matrix is reproduced on the final source. It is not terminal
GO: the coordinator owns one fresh full-suite and rehearsal decision, whose outcome
alone closes or truthfully rejects `m18.3`.
