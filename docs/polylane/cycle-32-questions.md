# Cycle 32 emergent questions

1. Should the absolute-worktree fixture derive every expected lane and poll path
   through one test helper so future runtime canonicalization cannot leave a mixed
   relative/absolute assertion set?
2. Should the coordinator-owned-terminal sentence become a named prompt contract
   fragment, while keeping the rendered Block G phrase unique and byte-bounded?
3. If Cycle 33 exposes another terminal-only mismatch, which focused reproduction
   can distinguish a new source defect from stale terminal expectations without
   consuming a second gate?

Deeper next-round option: after a fresh terminal GO, run a disposable stranger-first
clone, dual-provider install, and unattended first-run study across one software and
one non-software repository, preserving exact onboarding and evidence friction.
