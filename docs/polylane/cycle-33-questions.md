# Cycle 33 emergent questions

1. Should `expected_terminal_gates` be documented in a versioned manifest schema
   so every generator and hand-written manifest shares the same integer contract?
2. Should final report generation mechanically quote both actual and expected gate
   counts from the verified proof to prevent prose/telemetry drift?
3. Can the next fresh terminal run prove both the default and explicit-one routes
   without introducing a second coordinator-owned terminal event?

Deeper next-round option: after the fresh terminal GO, run a disposable
stranger-first clone with both provider installs and one unattended software plus
one non-software project, preserving onboarding friction and evidence provenance.
