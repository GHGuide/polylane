# Cycle 18 skill suggestions

All recommendations are already installed and path-resolved; no download or install is
needed. Each maps directly to this lane's frozen activity.

## `runtime-resilience`

- `superpowers:systematic-debugging` — reconstruct the exact ENOSPC and resume sequence
  before changing recovery policy.
- `engineering:testing-strategy` — design failpoint tests that prove atomic history,
  truthful reporting, and no false tmux input.
- `engineering:incident-response` — preserve evidence, stop retry storms, and distinguish
  product failure from host-resource failure.

## `skill-context`

- `superpowers:test-driven-development` — first prove runner-built prompts omit selected
  paths, then close that executable contract.
- `engineering:documentation` — make the exact read/fingerprint/use receipt unambiguous
  without bloating every builder prompt.
- `operations:process-optimization` — remove the name-only/compile-selected split so the
  planned skill kit actually reaches execution.

## Integrator

- `superpowers:verification-before-completion` — keep the terminal matrix at the host
  boundary and reject cached or partial evidence.
- `engineering:code-review` — adversarially inspect disk, resume, prompt, and graph seams.
- `ponytail:ponytail-review` — remove recovery complexity that does not buy a tested
  failure mode.
