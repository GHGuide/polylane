# Cycle 41 emergent questions

No user decision is required before Cycle 42A. These questions are now executable
contract and test work rather than reasons to pause:

1. Can one strict evidence DAG make fixture trust absorbing and compute every claim from
   the least-trusted transitive ancestor without schema or producer laundering?
2. Can exactly 20 independent brief outcomes, 15 wins, Wilson 95% lower bound above
   0.50, and exact two-sided sign p at most 0.05 be frozen before seeing any result?
3. Can metadata-only selection reserve all 24 calibration pairs per domain before
   downloading exactly 252 chosen identities, while resolving duplicate content and
   preserving valid negative z-scores?
4. Can request receipts prove the exact prompt bytes reached the declared model and bind
   the resulting build, capture, stimulus, ballot, and revision without trusting paths
   or environment labels?
5. Can five reusable configurations across two provider families keep `config_id`,
   `judge_instance_id`, and `session_id` distinct while preserving blinded independence?
6. Can the runner persist `WORKING -> HANDOFF_PENDING -> HANDOFF_COMMITTED -> QUIESCING
   -> DONE`, detect elapsed stalls independently of pane churn, and never write or mutate
   a worker's handoff?
7. Can unresolved external evidence yield `EXTERNAL-EVIDENCE-OPEN` after autonomous
   proof passes, without spawning a model repair or blocking unrelated autonomous work?
8. Can hard call, storage, staging, retry, and retention limits keep the worst campaign
   below 18,135,637,039 bytes and stop safely before the 5 GiB reserve is crossed?

