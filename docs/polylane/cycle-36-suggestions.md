# Cycle 36 skill suggestions

- **Selected — `superpowers:test-driven-development` (installed):** preserve the exact
  live failure as a red compiler regression before changing production code.
- **Selected — `engineering:debug` (installed):** trace the valid sentinel from agent
  output through prompt compilation to the runner's canonical parser.
- **Integrator — `engineering:code-review` and
  `superpowers:verification-before-completion` (installed):** review the two-file trust
  boundary and require observed focused evidence before GO.
- **External/GitHub: none.** This is a deterministic local protocol defect; another
  dependency would not improve the result.
