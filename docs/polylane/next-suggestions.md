# Post-goal suggestions

The first four were selected by the cycle-9 council and implemented as the cycle-10 perfection milestone. The rest are informational release experiments; they do not keep the completed loop running.

- [implemented] Deduplicate explicitly equivalent terminal acceptance gates with stable keys.
- [implemented] Make runner, state, and dashboard share one DONE and session authority.
- [implemented] Root learned outcomes at the canonical project instead of a lane worktree.
- [implemented] Give isolated lanes an atomic cross-worktree coordination relay.
- Capture real per-lane Codex token telemetry without parsing unstable terminal decoration.
- Add Linux CI coverage for Bash 3.2 compatibility and tmux lifecycle behavior.
- Add a held-out benchmark split that release tuning cannot inspect.
- Compare static model presets with outcome-learned lane-shape routing.
- Add an opt-in browser journey judge for applications with a web interface.
- Add an opt-in accessibility judge with project-specific severity thresholds.
- Add a signed mobile-build terminal acceptance recipe.
- Add a deployment canary adapter for projects that declare a staging environment.
- Add a reproducible network-failure fixture for research and dependency lanes.
- Add process-death fuzzing for the coordination lock and append protocol.
- Add a bounded disk-pressure test for long marathon runs.
- Add a schema migration test for every durable JSON and JSONL artifact.
- Add benchmark cases for localization and right-to-left interfaces.
- Add benchmark cases for offline-first conflict resolution.
- Add benchmark cases for authorization and multi-tenant data isolation.
- Add benchmark cases for large-data import and export workflows.
- Calibrate deterministic and model judges against the same curated defect set.
- Record why a skill helped, remained unused, or hurt with linked evidence.
- Decay stale skill outcomes when a skill version changes materially.
- Surface confidence intervals once enough benchmark samples exist.
- Add a control-room view of pending coordination requests and held claims.
- Add a release note generator sourced only from verified cycle evidence.
- Add an opt-in artifact-signing step after the terminal gate.
- Add a fault-injection rehearsal for a lost tmux server during integration.
- Add a repository-size stress corpus for scope and seam scanners.
- Publish a small public benchmark leaderboard only after real telemetry is trustworthy.
