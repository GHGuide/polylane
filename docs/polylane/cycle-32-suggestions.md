# Cycle 32 per-lane skill suggestions

No install is recommended; the defect is fully localized and the required skills
are already installed.

## Builder: `terminal-contract-repair`

1. `superpowers:systematic-debugging` — preserve the distinction between stale
   assertions and correct runtime behavior.
2. `superpowers:test-driven-development` — reproduce the five red assertions,
   make the smallest contract edits, and prove focused green.

## Integrator

3. `engineering:code-review` — audit that no production semantics or terminal
   boundary were weakened.
4. `superpowers:verification-before-completion` — require exact-tip focused proof,
   zero restarts, and zero terminal gates before GO.

GitHub suggestions are declined because a new dependency cannot improve two local
test/documentation contract edits.

