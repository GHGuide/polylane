# Cycle 20 digest — live seams fixed, certification remains NO-GO

Cycle 20 truthfully ended NO-GO after one builder restart; its READY integrator handoff entered the host boundary once, failed the restart-efficiency proof, skipped full terminal acceptance, and retained all worktrees. Live execution exposed three mechanisms now fixed: `763fb00` adds safe single-candidate status-marker normalization plus compiler-injected relay/DONE contracts, while `e1de56a` keeps nonce-scoped gate evidence outside completed worktrees, verifies the current proof run id, attributes host failures correctly, and never reports unknown Codex cost as zero. Red-first and broader matrices passed 381/0 plus 261/0, documentation truth 25/0, ShellCheck, marker consistency, and diff hygiene. Next: start Cycle 21 from `e1de56a` and require zero restarts, one host boundary, one full terminal acceptance, cleanup, and hermetic GO plus NO-GO rehearsal before closing `m20.1`, `m18.3`, and `c56`.
