# Cycle 21 research — primary evidence and routing

## Confirmed Cycle 20 causes

The Cycle 20 manifest and authored builder prompt both assigned
`docs/status-restart-accounting.md`, while the observer required
`docs/status-restart-accounting-audit.md`. The worker therefore followed the plan and
the coordinator spent one restart on an impossible contract. The integrator prompt also
said only “canonical relay,” leading the worker to read the post-cycle summary instead
of `.polylane/coordination.jsonl`. Finally, the host gate wrote proof into a completed
worktree and the report attributed the resulting NO-GO to the integrator with unknown
Codex cost shown as zero.

The repaired tip prevents each class mechanically: exact canonical marker ownership and
prompt-path lint happen before model launch; compiled prompts inject the literal relay
command after optimization; one clean committed current-nonce marker can be normalized
only as bounded recovery; host proof is canonical and nonce-scoped; and host failures
and unknown cost are reported truthfully.

## Fresh-preflight findings

An attempted supervisor help probe exposed that `--help` was treated as a manifest and
that `TERM`/`INT` released the lock but resumed the loop. Commit `dabd6f0` adds inert
help plus child signal propagation and passed 32/32 supervisor tests. Goal-tree audit
also found that host-only criteria had no mechanical finalizer. Commit `864050d` adds
validated `target_criteria`, keeps them open through promotion, and closes them only
after cleanup plus final efficiency proof; focused contract, dry-run, orchestration,
parity, docs, marker, and whole-tree ShellCheck gates pass.

All four remaining autonomous subgoals must be targets; otherwise the runner correctly
defers the terminal tier because work remains outside the wave. The Cycle 17 and Cycle
18 focused integration commands are byte-identical and now share one acceptance key.
The Cycle 16 and Cycle 18 terminal commands already share one key. This preserves frozen
commands while preventing duplicate expensive execution.

No internet research or new installation is useful for this evidence-only wave. The
highest-value evidence is a fresh process nonce, authoritative graph ledger, primary Git
history, deterministic tests, run telemetry, and hermetic GO/NO-GO rehearsal.
