# Cycle 28 council

## Evidence

- Product/runtime: continue. The worker tip contains useful focused repairs but
  the run's eight restarts disqualify efficiency certification.
- Reliability: continue. A demonstrably active command was treated as command
  churn by a second watchdog that did not share active-command state.
- Scope/integration: continue. The scope gate correctly blocked one planned path
  omitted from ownership, then the retry loop wasted work on a deterministic
  terminal mismatch.
- Economy: continue narrowly. Preserve the exact tip; do not repeat its completed
  implementation or run the terminal suite until the new guards are integrated.
- Truth: retain HALTED. Late commits are source inputs for a new nonce, never a
  retroactive Cycle 28 verdict.

## Decision

Elect `m26.1` as the next highest-weight route, with `m26.2`–`m26.4` in the same
coupled runtime lane. Cycle 29 runs focused acceptance only. Cycle 30 owns fresh
zero-restart terminal evidence.

