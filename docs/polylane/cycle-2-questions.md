# Cycle 2 questions

No product-direction question needs user input. Autonomous defaults: make graph mode
authoritative for contract-v2 manifests and preserve an explicit shadow/off escape hatch;
keep JSONL as the audit truth; permit only a disposable, hash-checked replay checkpoint;
prefer Codex `workspace-write` plus the narrow canonical Git metadata `--add-dir` over
full filesystem access; and treat missing owned tmux runtime as a recoverable runner
failure rather than an infinite poll.

