STATUS: runner-wiring DONE run=c39-visual-loop-20260812-a1

Wired current-UI authoritative visual tournament/certificate into the runner:
fail-closed preflight before any worktree/tmux side effect; host-owned
authoritative gate against the integrator worktree that a prose GO cannot
bypass; grounded-only bounded repair (<=2, durable across resumes, record +
artifact hashes must change); and project taste memory updated only after a
verified promotion from the exact promoted receipt. Legacy and non-UI runs are
byte-for-byte unchanged (explicit `contract_version` classification, no date
inference). Helpers stay behind overridable command variables; the runner never
re-derives their schemas.

Owned files: bin/polylane-run.sh, tests/test-taste-runner-gate.sh,
docs/verify-runner-wiring.md, docs/status-runner-wiring.md.

Verification: tests/test-taste-runner-gate.sh 43/0 (RED first: 20/22 → 43/0);
runner-focused regression group all 0 fail (visual-quality 7, visual-loop
28, visual-intelligence 9, visual-capture 19, promote 4, verify-gate 6,
advanced-runtime 24, graph-authority 56, session-resume 8, runtime-recovery 26);
shellcheck -S warning bin/polylane-run.sh PASS; bash -n ok; git diff --check clean.

Relay: handled prompt-contract (seq5, aligned agreement reader), a11y-evidence
(seq14, no runner change), quality-adapter (seq15, confirmed `authoritative`
alias + two field semantics). Open seams recorded in docs/verify-runner-wiring.md
DEFERRED: taste-memory record CLI unconfirmed and current-version literal
pending — both isolated behind runner override points.
