STATUS: integrator DONE run=c17-recovery-cert-20260809-a1

Both verified Cycle-17 recovery tips are merged as ancestors. Fresh cached focused and
cross-contract evidence is recorded in `docs/verify-integration.md`; the sole unchecked
acceptance is the coordinator-owned terminal matrix for `m16.4`, with `m17.3` retained
at that host boundary.
