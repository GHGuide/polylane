STATUS: engine DONE

All 8 locked goals shipped in bin/polylane-run.sh with evidence in docs/verify-engine.md:
shellcheck-clean (0.11.0, exit 0) · --resume · checkpoint-before-retry · per-lane poll
status line · usage-limit stall detection (notify once, no respawn) · tokens+cost in
report · --push · pipe-pane lane logs + notify hook wiring. Contracts intact (frozen
CLI/DONE/report/manifest; additive flags only). 32/32 harness tests pass on bash 3.2.
