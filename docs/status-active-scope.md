STATUS: active-scope DONE run=c29-active-scope-20260811-a1

Implementation: `0eb2b88` (`fix runtime active scope recovery`).

Verified focused runtime recovery, source-root canonicalization, terminal scope handling, planned-write admission, prompt linting, inherited Cycle 28 matrices, and final ShellCheck/diff checks. See `docs/verify-active-scope.md`.
